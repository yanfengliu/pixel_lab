# Lessons — evidence

The context, rule, and pointer behind each line in [lessons.md](lessons.md). Not session-start reading: open an entry when its rule is in doubt, or when the work is in that area.

Append durable engineering lessons here. Each entry should teach a future agent something that is not obvious from the current code — a trap, a non-obvious invariant, or a rule that keeps biting if ignored. One entry per lesson, newest at the top. Keep entries short; link to code or devlog rather than restating.

An entry is deleted together with its one-line rule the moment the lesson becomes a gate, and the claim is rewritten into that gate's own header — see [gate-proofs.md](gate-proofs.md) for the retired lessons and the mutation that proved each gate real.

Format:

```
## <short title> — YYYY-MM-DD
Context: when this came up.
Lesson: the durable rule or trap, phrased so it transfers to future work.
Pointer: devlog entry, file, or test that illustrates it.
```

---

_(no entries — all lessons to date have graduated to gates or to the constitution)_
