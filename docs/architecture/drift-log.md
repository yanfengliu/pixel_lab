# Architecture drift log

Append a row any time the code drifts from ARCHITECTURE.md or a KAD. Each
row either explains the temporary drift and its cleanup plan, or is the
trigger to update ARCHITECTURE.md / decisions.md.

| Date | What drifted | Why | Remediation |
|------|--------------|-----|-------------|
| 2026-04-24 | Initial draft of ARCHITECTURE.md placed the Zustand store in `src/app/` and PNG decoding in `src/io/`, while the first implementation put the store in `src/app/store.ts` and PNG encode/decode in `src/core/png.ts`. | Code-reviewer feedback pointed out the resulting `ui → app` cycle. PNG codec belongs in `core` because it's a pure codec with no DOM dependency and is needed by `core/export` to produce atlas bytes. | Moved store to `src/ui/store.ts`; left PNG in `src/core/png.ts` and updated ARCHITECTURE.md to match. No outstanding drift. |
| 2026-04-24 | `Source.kind` was `'sheet' \| 'gif'`; ARCHITECTURE.md § Data model described the GIF case by name. | Pixel-drawing feature needed blank animations (`kind: 'sequence'` with `importedFrom: 'blank'`), which share runtime semantics with imported GIFs but have no GIF file behind them. Locking the kind to a file format forced awkward `if kind === 'gif'` branches. | KAD-006 renames kind to `'sheet' \| 'sequence'`; provenance moves to `Source.importedFrom`. ARCHITECTURE.md § Data model updated. No outstanding drift. |
| 2026-04-24 | `Source.imageBytes` was the sole source of truth for a source's pixels. | Pixel-drawing adds per-frame editing; overwriting `imageBytes` would lose the original asset. | KAD-007 adds `Source.editedFrames?: RawImage[]` as a layer above `imageBytes`. When present, it is authoritative; otherwise fall back to decoding `imageBytes`. ARCHITECTURE.md § Data model notes both fields. No outstanding drift. |
| 2026-04-25 | `manifest.json` v1 emitted `w/h` for frame rects and `fps` (or per-frame `durationMs`) on animations. ARCHITECTURE.md § Export pipeline described v1 fields. | Cross-repo unification with idle-life made the field-name and timing-mode mismatches load-bearing: every consumer would otherwise need a translation layer. | KAD-008 bumps the schema to v2 (`width/height`, always per-frame `durationMs`, `version: 2`). ARCHITECTURE.md § Export pipeline updated. No outstanding drift. |
| 2026-04-30 | Doc audit pass: AGENTS.md referenced `docs/api-reference.md` and a `design/` directory that do not exist; debugging template referenced `npm run test:browser` and `docs/engine-feedback/current.md` (neither exists); `docs/devlog/summary.md` and `docs/learning/lessons.md` pointed at `docs/reviews/full/...` (the legacy review tree, migrated to `docs/threads/done/full/...`); summary still mentioned the retired `agent/<task>` branch convention. | Drift accumulated across multiple branch/structure migrations (store relocation to `src/ui/`, threads-convention adoption, removal of the API-reference doc) without follow-through fixes in instructional docs. | This pass: rewrote the API-surface checklist in AGENTS.md to point at the actual barrels (`src/core/index.ts`, `src/core/serialize/manifest-types.ts`); pointed the debugging template at real npm scripts (`npm test`, `npm run smoke`); rewrote `docs/devlog/summary.md` and refreshed `docs/learning/lessons.md` paths. Detailed devlog entries (under `docs/devlog/detailed/`) and the superpowers archive intentionally retain their original branch/path strings as historical context. No outstanding drift. |
